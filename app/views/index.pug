extends layout
block content
  // Main container
  main
    section#hero
        h1 ALL ITEMS
        label.toggle
          input(type='checkbox')
          span.slider
          span.labels(data-on='LOST' data-off='FOUND')
  // Sidebar container
  #sidebar
    h1 Categories
    ui
      a(href='#') All Categories
      a(href='#') Clothing
      a(href='#') Electronics
      a(href='#') Sport Equip.
            a(href='#') Student ID
      a(href='#') Other
  // Content container
  #content
    .sort
      i.fa-solid.fa-location-dot(style='font-size: 15px')      Sort By
      button
        | Date Posted 
        i.fa-solid.fa-chevron-down
        
    .posts
      // Posts
      .post-box
        h1 NAME OF ITEM
        .post-img
        form(action='/upload', method='post', enctype='multipart/form-data')
          label(for='image') Select an image to upload:
          input(type='file', name='image', id='image', accept='image/*')
          .button-upload 
            button(type='submit') Upload Photo

        .post-text
          i.fa-solid.fa-location-dot(style='font-size: 13px')      Location
          input#start(type='date' name='trip-start' value='2018-07-22' min='2018-01-01' max='2018-12-31')

      .post-box
        h1 NAME OF ITEM
        .post-img
        form(action='/upload', method='post', enctype='multipart/form-data')
          label(for='image') Select an image to upload:
          input(type='file', name='image', id='image', accept='image/*')
          .button-upload
            button(type='submit') Upload Photo

        .post-text
          i.fa-solid.fa-location-dot(style='font-size: 15px')      Location
          input#start(type='date' name='trip-start' value='2018-07-22' min='2018-01-01' max='2018-12-31')


